# CorpusProject
This is an attempt at creating some basic tools for corpus linguistics. 

Create a local venv and install required packages with ```pip install -r requirements.txt```. If you use Anaconda, use ```conda install --file requirements.txt```.

To start the app, run ```streamlit run StreamApp.py``` in the terminal. 